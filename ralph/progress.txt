2026-03-02: Scaffolded Flutter package with lib/, test/, and example/, passing flutter analyze and flutter test in root and analyze in example/.
2026-03-02: Implemented TutorialBubble solid background color with default and custom color tests; updated example to show colored bubble; all analyzes and tests passing in root and example/.
2026-03-02: Made package publishable on pub.dev by adding LICENSE, README, and CHANGELOG; flutter pub publish --dry-run now passes.
2026-03-02: Added TutorialBubbleSide enum and TutorialBubbleOverlay to position bubbles on configurable sides (including automatic side selection) with widget tests; flutter analyze/test and example analyze all passing.
2026-03-02: Updated example app to show a TutorialBubbleOverlay pointing at a target button, keeping the code minimal; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Ensured TutorialBubbleOverlay keeps bubbles fully on-screen near edges with improved clamping logic and new widget tests; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added gradient background support to TutorialBubble and TutorialBubbleOverlay with precedence over solid color, updated example to show a color fade bubble, and ensured flutter analyze/test in root and analyze in example all pass.
2026-03-02: Verified any Flutter widget (ElevatedButton, Text, and a custom widget) can be targeted without changing its implementation by measuring its layout rect and driving TutorialBubbleOverlay; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Introduced TutorialTextBubble for configurable text content and styles (including full TextStyle override), added widget tests, and updated the example to use it; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added a configurable dark overlay that dims the background while keeping the target and bubble bright using a custom painter in TutorialBubbleOverlay, plus a widget test; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added TutorialStep and TutorialEngineController to accept an ordered list of steps with target keys and bubble builders, plus tests and PRD update; flutter analyze/test and example analyze to be confirmed.
2026-03-02: Added an optional configurable halo/glow around TutorialBubble (including via TutorialBubbleOverlay), with new widget tests and updated example; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Enabled optional spring-like tap scale animation on TutorialBubble with widget tests; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added an optional arrow between TutorialBubbleOverlay and its target with a toggleable API and widget tests; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Arrow now follows a smooth quadratic Bezier curve from bubble side to target with arrowhead at target end; PRD "Arrow follows a smooth curved path" marked complete; analyze/test and example analyze passing.
2026-03-02: Arrow direction now matches bubble side: TutorialBubbleOverlay is StatefulWidget; position delegate reports resolved side in automatic mode via post-frame callback; arrow painter uses resolved side so arrow points correctly for top/bottom/left/right; PRD "Arrow direction automatically matches the bubble's side" marked complete; flutter analyze, test, and example analyze passing.
2026-03-02: Adjusted arrow geometry so the arrowhead tip lands exactly at the target border without overlapping the target interior; PRD "Arrow tip lands precisely at the target's border without overlapping it" marked complete; flutter analyze/test in root and analyze in example all passing.

2026-03-02: Extended TutorialEngineController with currentIndex/currentStep, isLastStep/isFinished, and an advance() API plus tests so engine steps run in order when completion is signaled; flutter analyze/test in root and example analyze all passing.
2026-03-02: Updated TutorialBubbleOverlay to block interactions outside the highlighted target using an interaction blocker layer and added a widget test to verify outside taps are absorbed; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added programmatic skip() and finish() controls to TutorialEngineController with new unit tests so callers can advance, skip, or finish tutorials imperatively; flutter analyze/test in root and example flutter analyze all passing.
2026-03-02: Added optional configurable halo/glow around the TutorialBubbleOverlay arrow via new arrowHalo parameters and TutorialArrowPainter, with a widget test verifying halo configuration; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Ensured tapping the target during the tutorial triggers its normal behavior by adding a widget test that verifies the target button still fires while outside taps remain blocked; flutter analyze/test in root and analyze in example to be confirmed.
2026-03-02: Extended TutorialEngineController with totalSteps and a ValueNotifier-based currentIndexListenable that fires when the active step changes, plus a unit test; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added an optional configurable halo around the highlighted target via TutorialBubbleOverlay targetHalo properties and a new TutorialTargetHaloPainter with tests, keeping the target fully interactive; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Introduced a TutorialEngine widget that overlays steps from TutorialEngineController and now hides the overlay when the final step completes or when the tutorial is finished early, with widget tests; flutter analyze/test in root and analyze in example all passing.
2026-03-02: Added a widget test showing TutorialEngine spanning multiple Navigator routes via MaterialApp.builder so the overlay sits above the Navigator while steps target widgets on different screens; updated PRD \"Tutorial flow can span multiple screens\" to passes and confirmed flutter analyze/test in root and analyze in example all pass.
2026-03-02: Updated TutorialEngineController with an explicit start() API and isStarted state plus a widget test so the TutorialEngine overlay remains hidden until the controller is started; marked the PRD item \"Engine does not show until explicitly started\" as passing.
2026-03-02: Added optional advance-on-bubble and advance-on-overlay-tap behaviors to TutorialEngine and TutorialBubbleOverlay with new widget tests, and marked the PRD item \"Tapping the bubble or dark background can optionally advance the tutorial\" as passing.
2026-03-02: Verified standalone spotlight mode by using TutorialBubbleOverlay and TutorialTextBubble directly (no TutorialEngine or controller) in both tests and the example app, and marked the PRD item \"Bubble and overlay work as a standalone spotlight without any tutorial engine\" as passing; flutter analyze/test in root and example analyze to be confirmed.
2026-03-02: Updated TutorialEngine to keep retrying target resolution so it gracefully waits for a step's target to appear on screen, added a widget test, and marked the corresponding PRD item as passing; flutter analyze/test in root and example analyze all passing.
2026-03-02: Added TutorialVisuals configuration with global defaults and per-step overrides for bubble color, overlay color, arrow visibility, halo settings, and text style in TutorialEngine, plus unit and widget tests; marked the PRD item "Visual parameters have global defaults and can be overridden per step" as passing and confirmed flutter analyze/test in root and analyze in example all pass.